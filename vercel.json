{
  "version": 2,
  "builds": [
    {
      "src": "package.json",  // Alterado de vite.config.js
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/.*",
      "dest": "dist/index.html"
    }
  ]
}